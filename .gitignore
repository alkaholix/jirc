# Dependencies
node_modules/

# Build output
dist/
src-tauri/target/

# Logs
*.log
npm-debug.log*

# Editor / OS
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
Thumbs.db

# Local config / secrets
.env
.env.local
*.local

# Local mIRC help reference (copyright mirc.com — for syntax audit only)
mIRC Help.html
mIRC Help_files/

# Local parity test scripts (BV2 / Sockbot, from the old jirc-main converter).
# Kept locally for testing only — not part of the repo. Originals are in the
# OneDrive mIRC backup.
test-scripts/

# Local design mockups (throwaway UI previews).
window-mockups.html

# Local copy of the mIRC help file (fidelity reference; not part of the repo).
*.chm

# AI working guides — kept LOCAL only (loaded by Claude Code), not on the public repo.
CLAUDE.md
CLAUDE-OPUS-4.8.md

# Internal project docs — kept LOCAL only, not on the public repo. README.md stays public.
docs/*.md

# Local mSL knowledge base (reference only) — kept LOCAL, never uploaded.
mirckb-master/
